export function renderPlaceholderPage(title, description) {
  return `
    <section>
      <header class="page-header"><div><h2>${title}</h2><p>${description}</p></div></header>
      <div class="limit-box">Modulo preparado para evoluir tela a tela com regras do seu fluxo.</div>
    </section>
  `;
}
